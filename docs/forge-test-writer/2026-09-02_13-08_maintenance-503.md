# Maintenance 503 CLI render

- **Date:** 2026-09-02 13:08
- **Coverage summary:** Feature — `errors.503` renders with no session error bag; `artisan down --render=errors::503` succeeds and `artisan up` always runs.
- **Failure scenarios included:** Missing `$errors` (the deploy path). HTTP login/guest screens still pass (error bag present).
- **Deliberately NOT tested & why:** Fallback `php artisan down` in deploy.sh is a shell `||`, not PHP. Not asserting Vite hashes in HTML (needs a built manifest, already covered by guest page tests).
- **Known gaps / next tests:** A deploy-script smoke test would need a shell fixture; out of scope.
- **Suite result:** `php artisan test --filter=MaintenancePageTest` — 2 passed. Related guest tests passed.
