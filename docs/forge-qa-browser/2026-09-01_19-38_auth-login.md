# Auth login — browser pass

- **Date:** 2026-09-01 19:38 +05
- **Trigger:** user asked to test `http://127.0.0.1:8080/auth/login` in the browser.
- **Environment:** Docker nginx `http://127.0.0.1:8080` (PHP 8.4.25). Live billing (`SOLA_FAKE=false`, `SOLA_FAKE_LOGIN=false` — not flipped this pass). Assets `public/build/manifest.json` 2026-08-31 20:22. No Cursor browser MCP; Google Chrome 152 headless via Puppeteer-core (`/usr/bin/google-chrome`), new profile. Viewports 1440×900 and 390×844. Console + failed requests captured on the page.
- **Account:** none. Guest only. One live `/identify` POST with `998900000000` (the fake-server “unknown” number).
- **Spec:** `docs/task/QA_CHECKLIST.md` §0, §A, §I (login-applicable rows), §J for `/auth/login`. Known-blocked `docs/task/BAJARILMAGAN_TASKLAR.md` (no auth rows). Prior QA: `2026-08-31_20-06_dashboard-layout-ux.md` (F-1 verify markdown still open).
- **Money / SMS:** no SMS authorised. A.3–A.8, A.10–A.12 not driven. A.2 probe used the documented fake unknown number against **live** billing.

## Checklist (before click)

0.1–0.5 env · A.1 form · A.2 unknown phone (one identify, no known subscriber) · A.9 guest `/finance` · matrix uz/ru/en × light/dark × 1440/390 · console/network · 390 overflow · F-1 verify copy only if verify reached.

## Coverage table

| § | Requirement | Verdict | Note |
|---|---|---|---|
| 0.1 | App URL answers | PASS | GET `/auth/login` 200 in Chrome; nginx 1.27.5 |
| 0.2 | Billing | PASS | Live identify answered (400/126 on the probe number). `SOLA_FAKE_LOGIN` stayed off |
| 0.3 | Assets current | PASS | `manifest.json` 2026-08-31 20:22 (motion/wait-state build) |
| 0.4 | Subscriber | N/A | Guest pass; no cabinet account |
| 0.5 | Console clean | PASS | 0 console errors, 0 failed page requests |
| A.1 | Login form | PASS | `name=login` `type=tel` `inputmode=tel` autocomplete=tel required autofocus; value `+998`; mask on; submit “Получить код” / “Kodni olish” / “Get the code” |
| A.2 | Unknown number → code 110 | NOT TESTED | Probe `998900000000` is **not** unknown on live `.201` — billing returned **126** (temporary, no PC access). See F-1. Stayed on `/auth/login`, no verify, no `account` cookie |
| A.3 | Correct number → SMS / verify | NOT TESTED | Would send a real SMS (`SOLA_FAKE_LOGIN=false`) |
| A.4 | Login throttle 5/1 | NOT TESTED | Needs repeated identify |
| A.5 | Wrong SMS | NOT TESTED | Verify not reached |
| A.6 | Verify throttle 10/1 | NOT TESTED | Verify not reached |
| A.7 | Multi-account picker | NOT TESTED | No verified session |
| A.8 | Account select → `/` | NOT TESTED | No verified session |
| A.9 | Guest `/finance` | PASS | Redirected to `http://127.0.0.1:8080/auth/login` |
| A.10 | Verified → `/auth/login` kicks to cabinet | NOT TESTED | Never verified |
| A.11 | Logout | NOT TESTED | Never verified |
| A.12 | Switch account | NOT TESTED | Never verified |
| I.1 | Date format | N/A | Login has no dates |
| I.2 / I.3 | Money / currency | N/A | Login has no amounts |
| I.5 | 390px no h-scroll | PASS | `scrollWidth === clientWidth === 390` |
| I.6 | 390px cards stack | N/A | `.u-auth-aside` is `display:none`; 1130 moves to footer |
| I.8 | 390px buttons full width | PASS | “Get the code” is `w-full` |
| I.9 | Brand colours | PASS | Current green tokens (not TZ “dark gray”); light + dark both used |
| Phone mask | `+998 90 123 45 67` | PASS | Typed `901234567` → `+998 90 123 45 67` |
| Progress + offline chrome | present | PASS | `[data-progress]` on page; offline banner in DOM and `hidden` while online |
| Auth orbs | present | PASS | `.u-auth-orbs` on 1440 and 390 |
| Prior F-1 | verify `**:phone` markdown | NOT TESTED | Verify not reached; strings still in `lang/{ru,uz,en}/app.php` `auth.send_phone` |

### Matrix `/auth/login`

| | uz | ru | en |
|---|---|---|---|
| dark 1440 | PASS (04) | PASS (03) | PASS (05) |
| light 1440 | NOT TESTED | PASS (01) | PASS with overlay open (06) |
| 390 | — | — | PASS light (07); aside hidden; 1130 in footer |

uz/light 1440 and ru/390, uz/390 not opened (lang cookie was `en` after the desktop sweep). Text-size radios not exercised.

## Findings

```
F-1 · HIGH · /auth/login (POST identify) · en · light · 1440px
Seen:      Toast: "Тип абонента ВРЕМЕННЫЙ не имеет доступа к ПК".
           Page copy stayed English. Stayed on /auth/login (no /auth/verify).
           Probe +998 90 000 00 00 (998900000000).
Expected:  A.2 — unknown number shows billing code 110 (“абонент не найден”)
           and no SMS. On live billing this number is a temporary subscriber
           (code 126), so 110 was not exercised.
           Separately: J untranslated strings — `lang/en/errors.php` and
           `lang/uz/errors.php` keys 110/121/126 are still the Russian
           sentences. `ErrorMessages::for(126)` therefore cannot be English
           or Uzbek.
Evidence:  evidence-2026-09-01-login/09-login-unknown-phone.png
           evidence-2026-09-01-login/facts.json step "A.2 body-snippet"
Owner:     forge-frontend-design (en/uz errors.php copy)
           forge-debugger only if identify 126 vs 110 on `.201` is unexpected
```

Carry-forward (not re-checked this pass):

```
Prior F-1 · HIGH · /auth/verify · ru · dark · 1440px  (2026-08-31_20-06)
Seen then: "Отправили код на **34567. …"
Still in repo: lang/*/app.php auth.send_phone contains `**:phone`
Owner:     forge-frontend-design (copy) / forge-debugger (mask)
```

No other visual FAIL on the login chrome (orbs, split aside at 1440, 1130, skip link in DOM, no 390 overflow, console clean).

## State changes made

| Action | Before | After |
|---|---|---|
| POST `/auth/login` once, phone `998900000000` (`+998 90 000 00 00`) | guest, no verify cookie | still on `/auth/login`; toast 126; input reset to `+998`; no `account` cookie; did not open verify |

No SMS intended. Whether billing queued an SMS before returning 126 is **unknown** (no billing log in this pass). App did not show the verify screen.

Lang cookie left at `en`; theme radio was clicked (dark then light). Display overlay was still open on shots 03 and 06.

## Not tested, and why

- A.3–A.8, A.10–A.12 — would send SMS or require a verified session. `SOLA_FAKE_LOGIN` was not flipped.
- A.2 with a number that live billing actually answers as **110**. `998900000000` is the fake-server unknown; on `.201` it is temporary (126).
- uz/light 1440; ru and uz at 390px.
- Empty submit (HTML `required` / mask leaving `+998` only).
- Text size 145% / print.
- Offline banner with the network actually dropped.
- Button `.is-loading` spinner (POST returned too fast to screenshot).
- Verify OTP paste / auto-submit (`otp.js`) — F-1 re-check.
- `/`, `/tariffs`, `/devices`, `/finance` (authenticated), `/statistics`, `/services`.

## What the next pass should re-check

- F-1 (this note): en/uz `errors.php` 110/126 after copy is translated; and A.2 with a CEO-supplied number that `.201` does not know (true 110).
- Prior F-1: verify lead after `**:phone` is removed — needs one authorised SMS or a documented `SOLA_FAKE_LOGIN` flip/revert.
- Light theme with the Display panel **closed** (06 still had the overlay).
- uz + ru at 390px overflow.
