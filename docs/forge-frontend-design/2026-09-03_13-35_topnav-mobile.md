# Topnav mobile responsive fix

- **Date:** 2026-09-03_13-35
- **Mode:** SaaS + Multi-market
- **Screens/components delivered**
  - `partials/topbar.blade.php` — single-row header on phones
  - `components/nav-balance.blade.php` — compact mobile balance chip
  - Topbar logo call — `h-8 sm:h-9` + `max-w-[8.5rem]` on xs
- **Decisions:** Dropped `flex-wrap` as the “last defence” because it dumped balance + settings onto a second line with empty left space. Kept one row; reclaimed width via smaller logo, hiding `ye` below `sm`, and truncating long figures. Large text (`:root[data-text]`) still wraps via existing CSS. Refused moving balance into the drawer — it must stay visible in the top row.
- **Quality floor result**
  - `npm run build` OK
  - `php artisan test --filter=Cabinet` — 68 passed
  - Target widths: 320–390px one row (logo may clip Wi-Fi Operator line via max-w)
- **Left for later**
  - Dedicated mark-only logo asset for xs (cleaner than clipping the lockup)
  - Visual check of 145% text mode on a real device
